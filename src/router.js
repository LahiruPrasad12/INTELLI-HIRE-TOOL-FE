import React from 'react';

import Auth_routes from './routes/auth_routes'

const Routers = () => {
    return (
        <div>
            <Auth_routes />
        </div>
    );
};

export default Routers;